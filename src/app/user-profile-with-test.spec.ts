import { UserProfileWithTest } from './user-profile-with-test';

describe('UserProfileWithTest', () => {
  it('should create an instance', () => {
    expect(new UserProfileWithTest()).toBeTruthy();
  });
});
